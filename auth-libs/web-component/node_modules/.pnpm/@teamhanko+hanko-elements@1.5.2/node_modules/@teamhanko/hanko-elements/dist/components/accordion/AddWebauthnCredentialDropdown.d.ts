import { StateUpdater } from "preact/compat";
import { HankoError } from "@teamhanko/hanko-frontend-sdk";
type CredentialType = "passkey" | "security-key";
interface Props {
    setError: (e: HankoError) => void;
    checkedItemID?: string;
    setCheckedItemID: StateUpdater<string>;
    onCredentialSubmit: (event: Event) => Promise<void>;
    credentialType: CredentialType;
}
declare const AddWebauthnCredentialDropdown: ({ checkedItemID, setCheckedItemID, onCredentialSubmit, credentialType, }: Props) => import("preact/compat").JSX.Element;
export default AddWebauthnCredentialDropdown;
