import { ComponentChildren } from "preact";
import { IconName } from "../icons/Icon";
import { UIAction } from "../../contexts/AppProvider";
type Props = {
    uiAction?: UIAction;
    title?: string;
    children: ComponentChildren;
    secondary?: boolean;
    dangerous?: boolean;
    isLoading?: boolean;
    isSuccess?: boolean;
    disabled?: boolean;
    autofocus?: boolean;
    showLastUsed?: boolean;
    onClick?: (event: Event) => void;
    icon?: IconName;
};
declare const Button: ({ uiAction, title, children, secondary, dangerous, autofocus, showLastUsed, onClick, icon, ...props }: Props) => import("preact").JSX.Element;
export default Button;
