import { ComponentChildren } from "preact";
type Props = {
    onSubmit?: (event: Event) => void;
    children: ComponentChildren;
    hidden?: boolean;
    maxWidth?: boolean;
};
declare const Form: ({ onSubmit, children, hidden, maxWidth }: Props) => import("preact").JSX.Element;
export default Form;
