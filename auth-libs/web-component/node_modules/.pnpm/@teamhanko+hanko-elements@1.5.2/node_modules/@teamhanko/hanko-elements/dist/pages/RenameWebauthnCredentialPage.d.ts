import { WebauthnCredential } from "@teamhanko/hanko-frontend-sdk/dist/lib/flow-api/types/payload";
type Props = {
    oldName: string;
    credential: WebauthnCredential;
    credentialType: "passkey" | "security-key";
    onBack: (event: Event) => Promise<void>;
    onCredentialNameSubmit: (event: Event, id: string, name: string) => Promise<void>;
};
declare const RenameWebauthnCredentialPage: ({ onCredentialNameSubmit, oldName, onBack, credential, credentialType, }: Props) => import("preact").JSX.Element;
export default RenameWebauthnCredentialPage;
