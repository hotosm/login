import { Flow } from "./Flow";
import { State } from "./State";
export default class BrowserFlowStorage {
    static save(state: State<any>, key?: string): void;
    static load(flow: Flow | Flow["fetchNextState"], key?: string): State<any> | null;
}
